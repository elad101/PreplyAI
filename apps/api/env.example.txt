# Server Configuration
PORT=3001
NODE_ENV=development

# PostgreSQL Database (Prisma format)
DATABASE_URL="postgresql://preplyai:preplyai_dev_password@localhost:5432/preplyai?schema=public"

# Firebase Admin SDK (for authentication only)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYourPrivateKeyHere\n-----END PRIVATE KEY-----\n"
# Note: In production on GCP, you can omit FIREBASE_CLIENT_EMAIL and FIREBASE_PRIVATE_KEY
# and use Application Default Credentials instead

# Google OAuth (for Calendar API)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3001/google/callback
GOOGLE_CALENDAR_SCOPES=https://www.googleapis.com/auth/calendar.readonly

# OpenAI
OPENAI_API_KEY=sk-proj-...

# Redis (for BullMQ job queue and caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Optional: Third-party enrichment provider
ENRICHMENT_API_KEY=
ENRICHMENT_PROVIDER=

# Security (REQUIRED: Use a secure random string of at least 32 characters)
SESSION_SECRET=your-secure-random-session-secret-at-least-32-characters-long

# Production-only: CORS allowed origins (comma-separated)
# ALLOWED_ORIGINS=https://yourapp.com,https://www.yourapp.com
